{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        // Needed because jest calls tsc in a way that doesn't
        // inline const enums.
        "preserveConstEnums": true
    },
}
